[package]
name = "oar-domain"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1.89"
chrono = "0.4.43"
thiserror = "2.0.18"
uuid = { version = "1.20.0", features = ["serde", "v4"] }
serde = { version = "1.0", features = ["derive"] }

